import logo from '../../../assets/images/Logo.png'

const Logo = () => {
    return (
        <div>
            <img className='h-10' src={logo} alt="" />
        </div>
    );
};

export default Logo;